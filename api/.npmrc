# The Node floor is a repository rule, not a suggestion: without this, npm downgrades an
# unmet `engines` to an EBADENGINE warning and installs anyway, which is how a contributor
# who skips dev-up.sh ends up on a Node the packages do not support.
engine-strict=true
