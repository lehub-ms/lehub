# Vite deliberately ignores .env.local in test mode, so tests get their origins from here.
# Committed on purpose: it is a fixture, not configuration.
VITE_API_BASE_URL=http://localhost:7071
VITE_ADMIN_BASE_URL=http://localhost:5174
