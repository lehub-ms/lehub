# Local development environment.
# Copy to .env (git-ignored) and change the password before sharing this workspace:
#   cp .env.example .env
#
# This is the single source of the local SQL password: docker-compose.yml reads it,
# and scripts/dev-up.sh propagates it into api/local.settings.json.

MSSQL_SA_PASSWORD=LocalDev@2026!
